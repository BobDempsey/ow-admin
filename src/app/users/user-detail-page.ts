import { Component } from '@angular/core';

/** A single user's view and edit screen. The user management task fills in its body. */
@Component({
  selector: 'app-user-detail-page',
  template: `
    <h1 tabindex="-1" class="text-2xl font-semibold text-slate-900 focus:outline-none">User</h1>
  `,
})
export default class UserDetailPage {}
